<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comtechnobyte.example.fitnesscareapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".underw"></activity>
        <activity android:name=".complited" />
        <activity android:name=".MainActivity" />
        <activity android:name=".report" />
        <activity android:name=".Shoutinstar" />
        <activity android:name=".plank" />
        <activity android:name=".Streaching" />
        <activity android:name=".DIF" />
        <activity android:name=".Ereport"/>
        <activity android:name=".Treadmill" />
        <activity android:name=".Currentsituation" />
            <activity android:name=".excersices" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>